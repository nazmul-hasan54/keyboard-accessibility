import { KeyBoardAccessibilityDirective } from './key-board-accessibility.directive';

describe('KeyBoardAccessibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyBoardAccessibilityDirective();
    expect(directive).toBeTruthy();
  });
});
